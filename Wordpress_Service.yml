apiVersion: v1
kind: Service
metadata:
     name: wordpress-service
     labels:
         app: wordpress
spec:
  type: ClusterIP
  selector:
       app: wordpress
  ports:
   -  protocol: TCP
      port: 80
      name: http
 
